var React = require('react');

var MyAccountSettings = React.createClass({
    render: function() {
        return (
            <div>
                <h1>settings</h1>
            </div>
        );
    }
});

module.exports = MyAccountSettings;
